SCRP_CharacterEvents = {
    ListRequested = 'scrp:characters:server:requestList',
    CreateRequested = 'scrp:characters:server:create',
    SelectRequested = 'scrp:characters:server:select',

    SendList = 'scrp:characters:client:sendList',
    CharacterSelected = 'scrp:characters:client:selected'
}